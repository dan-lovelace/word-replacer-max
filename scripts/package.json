{
  "name": "scripts",
  "version": "1.0.0",
  "scripts": {
    "build": "echo \"INFO: No build specified\" && exit 0"
  },
  "dependencies": {
    "@worm/shared": "*"
  },
  "devDependencies": {
    "@aws-sdk/client-ssm": "^3.664.0",
    "@worm/plugins": "*"
  }
}
